# Use the official npm registry
registry=https://registry.npmjs.org/

# Set the preferred package scope (replace `your-scope` with your actual scope)
# @your-scope:registry=https://registry.npmjs.org/

# Specify a proxy for outgoing network requests (if applicable)
# proxy=http://your-proxy-server:port

# Specify a proxy for HTTPS requests (if applicable)
# https-proxy=http://your-proxy-server:port

# Ignore SSL errors (not recommended for production)
# strict-ssl=false

# Disable automatic package-lock creation (if desired)
# package-lock=false

# Specify the location for globally installed packages
# prefix=/path/to/global-packages

# Set authentication tokens (if needed)
# //registry.npmjs.org/:_authToken=YOUR_AUTH_TOKEN
